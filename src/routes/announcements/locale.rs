/*use crate::domain::{language::Language, region::Region};

macro_rules! define_locale {
  ($name:ident, $lang:expr, $reg:expr) => {
    pub const $name: Locale = Locale {
      language: $lang,
      region: $reg,
    };
  };
}

pub struct Locale {
  pub language: Language,
  pub region: Region,
}

impl Locale {
  define_locale!(JAPANESE_JAPAN, Language::Japanese, Region::Japan);
  
}*/